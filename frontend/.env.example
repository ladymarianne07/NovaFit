# Frontend environment variables (Vite)
# Copy this file to .env.local for local overrides if needed

# Local development (recommended with Vite proxy)
# VITE_API_BASE_URL=/api

# Production (Vercel) -> point to your backend public URL
VITE_API_BASE_URL=https://YOUR_BACKEND_URL
