# Environment configuration
# Copy this to .env and update with your settings

# ⚠️ SECURITY: NEVER commit .env to git - it's in .gitignore for a reason!
# If you accidentally expose an API key, regenerate it immediately:
#   - GEMINI: https://aistudio.google.com/app/apikey
#   - USDA: https://fdc.nal.usda.gov/api-guide.html

# App settings
APP_NAME=NovaFitness API
VERSION=1.0.0
DEBUG=false

# Database (SQLite for MVP)
DATABASE_URL=sqlite:///./novafitness.db

# For PostgreSQL migration, uncomment and update:
# DATABASE_URL=postgresql://username:password@localhost/novafitness

# Security (CHANGE THESE IN PRODUCTION!)
SECRET_KEY=your-super-secret-key-change-this-in-production-make-it-long-and-random
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=525600

# CORS origins (add your tunnel URLs)
ALLOWED_ORIGINS=["http://localhost:3000", "https://localhost:3000"]

# Tunnel URL (set when using ngrok/cloudflare tunnel)
# TUNNEL_URL=https://abc123.ngrok.io

# USDA FoodData Central API key
# Get one at: https://fdc.nal.usda.gov/api-guide.html
# 🔒 Keep this secret - never share or commit to git
USDA_API_KEY=your-usda-api-key-here

# OpenFoodFacts (optional; public API but user-agent is recommended)
OPENFOODFACTS_USER_AGENT=NovaFitness/1.0 (contact: support@novafitness.local)

# FatSecret Platform (optional)
# Get credentials at: https://platform.fatsecret.com/
FATSECRET_CLIENT_ID=your-fatsecret-client-id-here
FATSECRET_CLIENT_SECRET=your-fatsecret-client-secret-here

# Google Gemini API (AI Studio) free tier
# Get key at: https://aistudio.google.com/app/apikey
# 🔒 Keep this secret - never share or commit to git
# If exposed, regenerate immediately
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash